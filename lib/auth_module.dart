library auth_module;

import 'package:flutter/material.dart';

class AuthModula extends StatelessWidget {
  const AuthModula({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}